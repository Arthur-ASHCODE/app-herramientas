RewriteEngine On

# Si se solicita un archivo o directorio existente,
# Apache lo entrega directamente.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Cualquier otra solicitud pasa por index.php.
RewriteRule ^ index.php [QSA,L]